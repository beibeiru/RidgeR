% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SecAct_wrappers.R
\name{SecAct.inference}
\alias{SecAct.inference}
\title{Secreted protein activity inference (Platform-aware wrapper)}
\usage{
SecAct.inference(
  Y,
  SigMat = "SecAct",
  lambda = 5e+05,
  nrand = 1000,
  ncores = NULL,
  rng_method = "srand",
  method = "auto",
  is.group.sig = TRUE,
  is.group.cor = 0.9
)
}
\arguments{
\item{Y}{Gene expression matrix (genes x samples).}

\item{SigMat}{Signature matrix: \code{"SecAct"} (bundled) or path to a tab-separated file.}

\item{lambda}{Ridge regularization parameter (default: 5e+05).}

\item{nrand}{Number of permutations (default: 1000).}

\item{ncores}{Number of CPU cores for multi-threaded variants (\code{NULL} = auto-detect).}

\item{rng_method}{RNG backend: \code{"srand"} (default, platform-dependent C stdlib)
or \code{"gsl"} (cross-platform GSL MT19937, matches SecActPy).}

\item{method}{Backend to use. One of \code{"auto"} (default), \code{"Tcol.mt"},
\code{"Tcol.st"}, \code{"Yrow.mt"}, \code{"Yrow.st"}, \code{"naive"}, or \code{"gsl.old"}.
\code{"auto"} selects the platform default.}

\item{is.group.sig}{Logical; group correlated signatures before regression (default TRUE).}

\item{is.group.cor}{Correlation threshold for signature grouping (default 0.9).}
}
\value{
A list with matrices \code{beta}, \code{se}, \code{zscore}, \code{pvalue}
of dimension (proteins x samples).
}
\description{
Unified entry point that automatically selects the best backend
for the current platform, or dispatches to a user-specified backend via the
\code{method} parameter. Platform defaults:
\itemize{
\item \strong{Linux}: \code{SecAct.inference.Tcol.mt} — T-column permutation, multi-threaded (fastest)
\item \strong{macOS}: \code{SecAct.inference.Yrow.st} — single-threaded (OpenMP unreliable without libomp, veclib)
\item \strong{Windows}: \code{SecAct.inference.Tcol.mt} — T-column permutation, multi-threaded (OpenMP by Rtools)
}
}
\examples{
\dontrun{
# Auto-select best backend for current platform
res <- SecAct.inference(expr.diff)

# Force a specific backend
res <- SecAct.inference(expr.diff, method = "Tcol.mt", ncores = 8)
res <- SecAct.inference(expr.diff, method = "gsl.old")
}
}
